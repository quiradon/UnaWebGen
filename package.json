{
  "name": "fortuna-web",
  "version": "1.0.0",
  "description": "A Static HTML page generator",
  "main": "index.js",
  "scripts": {
    "start": "bunx wrangler pages dev dist/ d1 create",
    "build": "bun run copy-static && bun run copy-root-files && bun run build.js",
    "test": "bun test",
    "copy-static": "bun run copyfiles -u 1 \"static/**/**/*\" dist/static",
    "copy-root-files": "bun run copyfiles manifest.json robots.txt dist/static",
    "dev": "bun run build && bun run start",
    "clean": "rimraf dist"
  },
  "author": "",
  "license": "ISC",
  "packageManager": "bun@1.0.0",
  "dependencies": {
    "cli-progress": "^3.12.0",
    "copyfiles": "^2.4.1",
    "express": "^4.19.2",
    "fast-glob": "^3.3.3",
    "fs-extra": "^11.3.0",
    "html-minifier": "^4.0.0",
    "http": "^0.0.1-security",
    "p-limit": "^6.2.0",
    "progress": "^2.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "wrangler": "^4.3.0",
    "rimraf": "^5.0.5"
  }
}
